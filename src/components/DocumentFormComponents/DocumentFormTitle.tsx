const DocumentFormTitle = ({ title }: { title: string }) => {
  return (
    <h1 className="mb-10 text-red-700 font-bold text-5xl text-center">
      {title}
    </h1>
  );
};

export default DocumentFormTitle;
